{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "pnpm exec tailwindcss -i assets/styles.css -o dist/assets/styles.css --minify",
    "build": "pnpm build:css"
  },
  "devDependencies": {
    "tailwindcss": "^4.1.0"
  }
}

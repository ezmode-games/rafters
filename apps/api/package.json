{
  "name": "@rafters/api",
  "version": "1.0.0",
  "description": "Rafters API service deployed on Cloudflare Workers",
  "type": "module",
  "private": true,
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.config.cloudflare.ts",
    "test:preflight": "vitest run && vitest run --config vitest.config.cloudflare.ts",
    "lint": "biome check .",
    "format": "biome format --write .",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@rafters/color-utils": "workspace:*",
    "@rafters/shared": "workspace:*",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.6.9",
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  },
  "author": "Sean Silvius",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/real-handy/rafters",
    "directory": "apps/api"
  }
}

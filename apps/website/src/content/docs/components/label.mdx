---
title: "Label"
description: "Accessible form label component"
order: 13
---

import ComponentDoc from '../../../components/docs/ComponentDoc.astro';
import CodeBlock from '../../../components/docs/CodeBlock.astro';

<ComponentDoc
  name="label"
  description="A semantic form label that creates proper associations between labels and form controls. Essential for accessibility and usability."
  examples={[
    {
      title: "Basic Label",
      description: "Associate a label with an input using htmlFor.",
      code: `import { Label } from '@/components/ui/label';
import { Input } from '@/components/ui/input';

<div className="grid gap-2">
  <Label htmlFor="email">Email</Label>
  <Input id="email" type="email" />
</div>`
    },
    {
      title: "With Required Indicator",
      description: "Show required fields with an asterisk.",
      code: `<div className="grid gap-2">
  <Label htmlFor="name">
    Name <span className="text-destructive">*</span>
  </Label>
  <Input id="name" required />
</div>`
    },
    {
      title: "Wrapping Pattern",
      description: "Label can wrap the input for implicit association.",
      code: `<Label>
  <span className="mb-2 block">Accept terms</span>
  <Checkbox />
</Label>`
    },
    {
      title: "With Disabled Input",
      description: "Label styling adapts to disabled peer state.",
      code: `<div className="grid gap-2">
  <Label htmlFor="disabled-field">Disabled Field</Label>
  <Input id="disabled-field" disabled className="peer" />
</div>`
    }
  ]}
  props={[
    { name: "htmlFor", type: "string", description: "ID of the associated form control" },
    { name: "className", type: "string", description: "Additional CSS classes" }
  ]}
/>

## Accessibility

- Uses native `<label>` element for proper semantics
- Clicking the label focuses the associated input
- Screen readers announce the label with the form control
- Supports `peer-disabled:` styling for disabled states

## Styling

<CodeBlock
  code={`// Base classes
text-sm font-medium leading-none

// Peer disabled state
peer-disabled:cursor-not-allowed peer-disabled:opacity-70`}
  language="css"
  filename="tokens"
/>

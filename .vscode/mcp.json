{
  "inputs": [
    {
      "type": "promptString",
      "id": "github_token_org_access",
      "description": "GitHub Personal Access Token (with real-handy org access)",
      "password": true
    }
  ],
  "servers": {
    "github": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "-e",
        "GITHUB_PERSONAL_ACCESS_TOKEN",
        "ghcr.io/github/github-mcp-server"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${input:github_token_org_access}"
      }
    }
  }
}
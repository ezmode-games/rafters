pre-commit:
  parallel: true
  jobs:
    - name: lint and format
      run: pnpm biome check --write

    - name: typecheck
      run: pnpm typecheck

pre-push:
  jobs:
    - name: preflight check
      run: pnpm preflight

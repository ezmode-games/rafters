---
"rafters": patch
---

Update CLI to use consolidated registry API endpoints

- Registry API consolidated from separate app to website `/api/registry`
- CLI now uses https://rafters.realhandy.tech/api/registry endpoints
- All component fetching updated for new API structure
- Maintains full compatibility with shadcn registry format
- Enhanced with Rafters design intelligence metadata